[project]
name = "Coursework job search on hh.ru"
version = "0.1.0"
description = "Курсовая работа в парадигме ООП - Поиск вакансий на hh.ru"
authors = [{name = "Fomenko Aleksey", email = "alexxsmr@yandex.ru"}]
readme = "README.md"
requires-python = "3.13.1"
dependencies = [
    "pytest (>=8.3.4,<9.0.0)",
    "requests>=2.31.0",
    "pandas>=2.0.0",
    "python-dotenv>=1.0.0",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"


[tool.flake8]
max-line-length = 119
ignore = ["E203", "W503", "E701"]
exclude =[".git", "__pycache__", "venv", ".venv"]


[tool.mypy]
disallow_untyped_defs = true
no_implicit_optional = true
warn_return_any = true
exclude = 'venv'


[tool.black]
# Максимальная длина строки
line-length = 119
# Файлы, которые не нужно форматировать
exclude = '''
(
  /(
      \.eggs         # Исключить несколько общих каталогов
    | \.git          # в корне проекта
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | dist
  )/
  | foo.py           # Также отдельно исключить файл с именем foo.py
                     # в корне проекта
)
'''


[tool.isort]
# максимальная длина строки
line_length = 119


[tool.coverage.report]
exclude_also = [
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "import",
    "from",
    "@abstractmethod"
]
